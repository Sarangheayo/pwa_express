{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "express",
            "program": "${workspaceFolder}/app.js",
            "restart": true,
            "skipFiles": [
                "${workspaceFolder}/node_modules/**",
            ]
        }
    ]
}